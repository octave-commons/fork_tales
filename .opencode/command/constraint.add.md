(prompt
  (meta (frontmatter (name "constraint/add") (description "Add a constraint (never remove)")) (tags "#ημ"))
  (constraints
    (add
      (rule
        (id "no-retcon-lore")
        (enabled true)
        (text "Do not retcon established lore; extend it via additive side-canons.")))))
