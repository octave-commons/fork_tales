{
  "name": "fork-tales-story-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "node build.mjs --root .. --out dist",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
