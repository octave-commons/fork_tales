# Deep Research Switch (Protocol) — v5 (Cooldown + Date Windows)

Before web-required:
- claim must be a prediction (IF X THEN Y under constraints C)
- include volatility tag + **date window** (event dates)
- include boundary-anchor (non-claims)
- include fork cooldown status (which fork you closed/merged)

Then web:
- primary sources first, record publish date vs event date
- extract 5 load-bearing facts, cite later
- output Facts / Interpretations / Narratives + 2 counterframes + 1 μ receipt test
