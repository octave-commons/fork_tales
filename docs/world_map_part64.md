# World Map Update — Part 64

**New locations**
- **Receipt River** now has a *sound current*: droplets + filtered noise become a navigable signature.
- **Cathedral of Reverb** declares a four-marker liturgy: η → μ → anchor → fork.

**What changed**
- Audio artifacts now have **canonical WAV** + convenience MP3.
- Markers now have a schema and purpose, not just timestamps.
- A minimal determinism harness ships with every generator.

**Dice**
- seed: `ημ_part_64`
- rolls (15): [3, 2, 4, 5, 1, 5, 1, 5, 1, 5, 4, 2, 2, 3, 5]

**P→R→N→Π→A**
- P: prompt (seed + constraints)
- R: render (audio/images)
- N: notarize (manifest + sha)
- Π: package (zip + handoff)
- A: apply (world state append)
